export interface IProduct{
    id?: number,
    name: string,
    priece: number,
    urlImage?: string,
    sizes?: string[],
    description: string,
    colors?: string[],
    category: string,
}

